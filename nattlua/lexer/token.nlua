local type Token = {
    @Name = "Token",
    type = string,
    value = string,
    start = number,
    stop = number,
    is_whitespace = boolean,
    whitespace = nil | {
        [number] = {
            type = string,
            start = number,
            stop = number,
        }
    },
}

return {
    Token = Token
}