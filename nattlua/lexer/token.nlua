local type Token = {
    @Name = "Token",
    type = string,
    value = string,
    start = number,
    stop = number,
    has_whitespace = boolean,
    whitespace = nil | {
        [number] = {
            type = string,
            start = number,
            stop = number,
        }
    },
}

return {
    Token = Token
}